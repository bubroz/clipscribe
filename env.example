# ClipScribe Environment Configuration
# Copy this file to .env and configure your settings

# -----------------------------------------------------------------------------
# AUTHENTICATION: Choose ONE of the two methods below.
# -----------------------------------------------------------------------------

# --- METHOD 1: Google AI Studio (Recommended for most users) ---
# Instructions:
# 1. Get your API key at: https://makersuite.google.com/app/apikey
# 2. IMPORTANT: On that page, link your API key to a Google Cloud project with billing enabled to get high rate limits.
# 3. Set USE_VERTEX_AI to false.

GOOGLE_API_KEY="your-google-api-key-here"
USE_VERTEX_AI=false


# --- METHOD 2: Vertex AI (Enterprise / Advanced users) ---
# Instructions:
# 1. Ensure you have authenticated via the gcloud CLI: `gcloud auth application-default login`
# 2. Provide the path to your service account JSON file.
# 3. Set USE_VERTEX_AI to true.

# Your Google Cloud Project ID
# VERTEX_AI_PROJECT="your-gcp-project-id"

# The location of your GCP project (e.g., us-central1)
# VERTEX_AI_LOCATION="us-central1"

# Path to your Google Cloud service account credentials file
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account.json"


# -----------------------------------------------------------------------------
# BASIC SETTINGS
# -----------------------------------------------------------------------------

# Output directory (default: output)
OUTPUT_DIR="output"

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# Maximum video duration in seconds (default: 3600 = 1 hour)
MAX_VIDEO_DURATION=14400

# Cost warning threshold in USD (default: 1.0)
COST_WARNING_THRESHOLD=1

# Temporal intelligence level: standard, enhanced (default: enhanced)
# 'enhanced' provides better extraction with a slight cost increase.
TEMPORAL_INTELLIGENCE_LEVEL=enhanced

# Video retention policy: delete, keep_processed, keep_all (default: delete)
VIDEO_RETENTION_POLICY=delete


# -----------------------------------------------------------------------------
# ADVANCED SETTINGS
# -----------------------------------------------------------------------------

# Maximum concurrent downloads for playlists (default: 3, max: 10)
# CONCURRENT_DOWNLOADS=3

# AI temperature for creativity (default: 0.3, range: 0.0-1.0)
# TEMPERATURE=0.3

# Gemini request timeout in seconds (default: 300 = 5 minutes)
GEMINI_REQUEST_TIMEOUT=300


# -----------------------------------------------------------------------------
# YT-DLP SETTINGS (for private videos)
# -----------------------------------------------------------------------------

# Path to a cookies file for accessing private content
# YTDLP_COOKIES_FILE="/path/to/cookies.txt"

# Proxy URL if needed
# YTDLP_PROXY="http://proxy.example.com:8080"
