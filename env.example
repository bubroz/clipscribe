# ClipScribe Environment Configuration
# Copy this file to .env and configure your settings

# === REQUIRED ===
# Get your API key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# === OPTIONAL: VERTEX AI (for better reliability) ===
# Set to true if you're getting 503 errors with regular Google AI
USE_VERTEX_AI=false

# Required if USE_VERTEX_AI=true
# VERTEX_AI_PROJECT_ID=your-gcp-project-id
# VERTEX_AI_LOCATION=us-central1

# === BASIC SETTINGS ===

# Output directory (default: output)
OUTPUT_DIR=output

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# Maximum video duration in seconds (default: 3600 = 1 hour)
MAX_VIDEO_DURATION=3600

# Cost warning threshold in USD (default: 1.0)
COST_WARNING_THRESHOLD=1.0

# Temporal intelligence level: standard, enhanced (default: enhanced)
# enhanced = better extraction with slight cost increase
TEMPORAL_INTELLIGENCE_LEVEL=enhanced

# Video retention: delete, keep_processed, keep_all (default: delete)
VIDEO_RETENTION_POLICY=delete

# === ADVANCED SETTINGS ===

# Maximum concurrent downloads (default: 3, max: 10)
# CONCURRENT_DOWNLOADS=3

# AI temperature for creativity (default: 0.3, range: 0.0-1.0)
# TEMPERATURE=0.3

# Gemini request timeout in seconds (default: 300 = 5 minutes)
# GEMINI_REQUEST_TIMEOUT=300

# === YT-DLP SETTINGS (for private videos) ===

# Path to cookies file for accessing private content
# YTDLP_COOKIES_FILE=/path/to/cookies.txt

# Proxy URL if needed
# YTDLP_PROXY=http://proxy.example.com:8080

# =================================================================
# GETTING STARTED
# =================================================================
# 1. Copy this file to .env: cp env.example .env
# 2. Add your Google API key above
# 3. Run: poetry install
# 4. Test: poetry run clipscribe process "https://youtube.com/watch?v=..."
# 5. For collections: poetry run clipscribe process-collection "name" "URL1" "URL2"
# ================================================================= 