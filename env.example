# ClipScribe v3.0.0 Environment Configuration
# Copy this file to .env and add your API keys

# =============================================================================
# REQUIRED: Provider API Keys
# =============================================================================

# Grok Intelligence (REQUIRED for all providers)
# Get key at: https://x.ai/api
XAI_API_KEY=your_xai_api_key_here

# =============================================================================
# TRANSCRIPTION PROVIDERS (Choose one or more)
# =============================================================================

# Voxtral Provider (Mistral API - cheap, no speakers)
# Get key at: https://console.mistral.ai
MISTRAL_API_KEY=your_mistral_api_key_here

# WhisperX Local Provider (Apple Silicon/CPU - FREE, speakers)
# Get token at: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=your_huggingface_token_here

# WhisperX Modal Provider (Cloud GPU - quality, speakers)
# Requires Google Cloud service account with Storage Admin role
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
GCS_BUCKET=your-gcs-bucket-name

# =============================================================================
# OPTIONAL: API Server Configuration (if running clipscribe-api)
# =============================================================================

# Redis (for job queue)
REDIS_URL=redis://localhost:6379/0

# Server
HOST=0.0.0.0
PORT=8000

# Authentication (comma-separated tokens)
VALID_TOKENS=tok_abc123,tok_def456

# Rate Limiting
TOKEN_MAX_RPM=60
TOKEN_MAX_DAILY_REQUESTS=2000
TOKEN_DAILY_BUDGET_USD=5.0
ADMISSION_ACTIVE_LIMIT=100

# CORS (comma-separated origins)
CORS_ALLOW_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# =============================================================================
# OPTIONAL: Output Configuration
# =============================================================================

# Output directory (default: output)
OUTPUT_DIR=output

# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO
